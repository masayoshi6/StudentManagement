<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="raisetech.StudentManagement.repository.StudentRepository">

  <!-- 受講生の全件検索　-->
  <select id="search" resultType="raisetech.StudentManagement.data.Student">
    SELECT * FROM students
  </select>

  <!-- 受講生の検索　-->
  <select id="searchStudent" resultType="raisetech.StudentManagement.data.Student">
    SELECT * FROM students WHERE id = #{id}
  </select>

  <!-- 受講生のコース情報の全件検索　-->
  <select id="searchStudentCourseList" resultType="raisetech.StudentManagement.data.StudentCourse">
    SELECT * FROM students_courses
  </select>

  <!-- 受講生IDに紐づく受講生コース情報の検索　-->
  <select id="searchStudentCourse" resultType="raisetech.StudentManagement.data.StudentCourse">
    SELECT * FROM students_courses WHERE student_id = #{studentId}
  </select>

  <!-- 受講生の新規登録。IDに関しては自動採番を行う。　-->
  <!-- @OptionsはINSERTするときにだけ付ける！！  -->
  <insert id="registerStudent" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO students(name, kana_name, nickname, email, area, age, sex, remark, is_deleted)
    VALUES(#{name}, #{kanaName}, #{nickname}, #{email}, #{area}, #{age}, #{sex}, #{remark},
    false)
  </insert>

  <!-- 受講生コース情報の新規登録。IDに関しては自動採番を行う。　-->
  <!-- @OptionsはINSERTするときにだけ付ける！！  -->
  <insert id="registerStudentCourse" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO students_courses(student_id, course_name, course_start_at, course_end_at)
    VALUES(#{studentId}, #{courseName}, #{courseStartAt}, #{courseEndAt})
  </insert>

  <!-- 受講生を更新　-->
  <update id="updateStudent">
    UPDATE students SET name = #{name}, kana_name = #{kanaName}, nickname = #{nickname},
    email = #{email}, area = #{area}, age = #{age}, sex = #{sex}, remark = #{remark},
    is_deleted = #{isDeleted} WHERE id = #{id}
  </update>

  <!-- 受講生コース情報のコース名を更新　-->
  <update id="updateStudentCourse">
    UPDATE students_courses SET course_name = #{courseName} WHERE id = #{id}
  </update>

  <!-- すべての受講生のコース情報の申込状況の全件検索　-->
  <select id="searchStudentApplicationStatus"
    resultType="raisetech.StudentManagement.data.StudentApplicationStatus">
    SELECT * FROM students_application_status
  </select>

  <!-- 受講生のコース情報の申込状況の新規登録（初回登録時は「仮申込」として登録される -->
  <!-- @OptionsはINSERTするときにだけ付ける！！  -->
  <insert id="registerApplicationStatus" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO students_application_status (student_course_id, status)
    VALUES (#{studentCourseId}, #{status})
  </insert>

  <!-- 受講生のコース情報の申込状況を更新 -->
  <update id="updateApplicationStatus">
    UPDATE students_application_status
    SET status = #{status}
    WHERE id = #{id}
  </update>

  <!-- 特定の文字から始まる名前の受講生を検索　-->
  <select id="findStudentsByNamePrefix" resultType="raisetech.StudentManagement.data.Student">
    SELECT * FROM students
    WHERE kana_name LIKE CONCAT(#{prefix}, '%')
  </select>

  <!-- 指定区間内の年齢を持つ受講生を検索 -->
  <select id="findStudentsByAgeRange" resultType="raisetech.StudentManagement.data.Student">
    SELECT * FROM students
    WHERE age BETWEEN #{minAge} AND #{maxAge}
  </select>

  <!-- 性別で受講生を検索 -->
  <select id="findStudentsByGender" resultType="raisetech.StudentManagement.data.Student">
    SELECT * FROM students
    WHERE sex = #{sex}
  </select>

  <!-- 指定したコースを受講している受講生のコース情報を検索 -->
  <select id="findStudentsByCourse" resultType="raisetech.StudentManagement.data.StudentCourse">
    SELECT * FROM students_courses
    WHERE course_name = #{courseName}
  </select>

  <!-- 受講開始日の期間を設定し、その期間内に受講がスタートする受講生のコース情報を検索 -->
  <select id="findCoursesByStartDateRange"
    resultType="raisetech.StudentManagement.data.StudentCourse">
    SELECT * FROM students_courses
    WHERE course_start_at BETWEEN #{from} AND #{to}
  </select>

  <!-- 受講生コース情報のオブジェクトとそれに紐づく受講生オブジェクトを集めたリストを返します。 -->
  <select id="findStudentsByIds" resultType="raisetech.StudentManagement.data.Student">
    SELECT * FROM students
    WHERE id IN
    <foreach item="id" collection="ids" open="(" separator="," close=")">
      #{id}
    </foreach>
  </select>

  <!-- 申し込み状況（仮申込、本申込、受講中、受講終了のいずれか）で受講生の検索を行います。 -->
  <select id="findStudentsByStatus"
    resultType="raisetech.StudentManagement.data.StudentApplicationStatus">
    SELECT * FROM students_application_status
    WHERE status = #{status}
  </select>

</mapper>